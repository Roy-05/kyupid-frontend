import styles from './Sidebar.module.scss';

const Sidebar = ({}) => {
    return (
        <div className={styles.sidebarRoot}>
            <div>Hi bRO</div>
        </div>
    );
};

export default Sidebar;
